# Employee Attrition Prediction System — Python dependencies
# Pinned to the exact versions the model in model.pkl was trained with,
# to avoid scikit-learn unpickling / feature-name mismatches in production.

Flask==3.1.3
Werkzeug==3.1.7
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.1

scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
threadpoolctl==3.6.0
joblib==1.5.3

# Production WSGI server
gunicorn==22.0.0
